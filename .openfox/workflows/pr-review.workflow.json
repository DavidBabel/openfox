{
  "metadata": {
    "id": "review",
    "name": "PR Review",
    "description": "Review, fix, and merge pull requests targeting develop.",
    "version": "1.0.0",
    "color": "#10b981",
    "parameters": [
      {
        "id": "pr_number",
        "label": "PR Number",
        "description": "The GitHub pull request number",
        "position": 0,
        "required": true
      }
    ]
  },
  "entryStep": "setup",
  "settings": {
    "maxIterations": 20
  },
  "steps": [
    {
      "id": "setup",
      "name": "Setup Workspace",
      "type": "agent",
      "phase": "build",
      "agentId": "builder",
      "prompt": "Set up the review workspace for PR #{{pr_number}}:\n\n1. Use the `workspace` tool to switch to a new workspace named `review-pr-{{pr_number}}`\n2. Point origin to GitHub: `git remote set-url origin git@github.com:co-l/openfox.git`\n3. Fetch and sync with develop: `git fetch origin && git reset --hard origin/develop`\n4. Checkout the PR branch: `gh pr checkout {{pr_number}}`\n\nIf any step fails, report the error and call step_done(). Otherwise call step_done().",
      "transitions": [{ "when": { "type": "always" }, "goto": "check_base" }]
    },
    {
      "id": "check_base",
      "name": "Verify Base Branch",
      "type": "agent",
      "phase": "build",
      "agentId": "builder",
      "prompt": "Verify the PR #{{pr_number}} targets develop:\n\n1. Check the base branch: `PR_BASE=$(gh pr view {{pr_number}} --json baseRefName --jq '.baseRefName')`\n2. If it's not \"develop\", retarget it: `gh api repos/co-l/openfox/pulls/{{pr_number}} -X PATCH -f base=develop && git fetch origin`\n3. Rebase onto latest develop: `git rebase origin/develop`\n\nIf rebase has conflicts, abort with `git rebase --abort` and report them. Otherwise call step_done().",
      "transitions": [{ "when": { "type": "always" }, "goto": "review_report" }]
    },
    {
      "id": "review_report",
      "name": "Review & Report",
      "type": "agent",
      "phase": "verification",
      "agentId": "builder",
      "prompt": "Review PR #{{pr_number}} and present findings:\n\n1. Read the diff: `git diff origin/develop...HEAD`\n2. List changed files: `git diff --stat origin/develop...HEAD`\n3. Run: `npm run test:unit && npm run typecheck && npm run lint`\n4. Inspect code quality, error handling, edge cases\n\nPresent a clear summary to the user:\n- What was reviewed\n- Key findings with specific line references\n- Whether any fixes are needed (be specific about what and why)\n- If no fixes needed, state that clearly\n\nCall step_done() when done.",
      "transitions": [{ "when": { "type": "always" }, "goto": "approve_fixes" }]
    },
    {
      "id": "approve_fixes",
      "name": "Approve Fix Plan",
      "type": "user",
      "phase": "verification",
      "transitions": [{ "when": { "type": "always" }, "goto": "apply_fixes" }]
    },
    {
      "id": "apply_fixes",
      "name": "Apply Fixes",
      "type": "agent",
      "phase": "build",
      "agentId": "builder",
      "prompt": "If the review found issues, apply fixes now. Otherwise call step_done() to skip.\n\nTo apply fixes:\n1. Run checks: `npm run typecheck && npm run lint && npm run test:unit`\n2. Commit: `git add -A && git commit -m \"review: fixes for PR #{{pr_number}}\"`\n3. Tag: `git tag review-fixes-{{pr_number}}`\n\nCall step_done() when done.",
      "transitions": [{ "when": { "type": "always" }, "goto": "start_dev_server" }]
    },
    {
      "id": "start_dev_server",
      "name": "Start Dev Server",
      "type": "agent",
      "phase": "build",
      "agentId": "builder",
      "prompt": "Start the dev server using the `dev_server` tool so the user can manually test the changes. Once it's running, present a handoff summary:\n\n> **PR #{{pr_number}} is ready at http://localhost:<port>.**\n>\n> **Metrics:** Tests X → Y (+Z), Typecheck ✅, Lint ✅\n>\n> **What I fixed:**\n> - _bullet list of specific changes_\n> - _why each matters_\n>\n> **What to test:**\n> - _specific things to try from the user's perspective_\n> - _edge cases_\n\nDo NOT ask the user if they want the dev server started — just start it and present the summary. Call step_done() when done.",
      "transitions": [{ "when": { "type": "always" }, "goto": "user_test" }]
    },
    {
      "id": "user_test",
      "name": "User Testing",
      "type": "user",
      "phase": "verification",
      "transitions": [{ "when": { "type": "always" }, "goto": "merge" }]
    },
    {
      "id": "merge",
      "name": "Merge PR",
      "type": "agent",
      "phase": "build",
      "agentId": "builder",
      "prompt": "Merge PR #{{pr_number}} and clean up:\n\n1. Push our fix tag to origin (workspace objects aren't visible from the original repo):\n   `git push origin review-fixes-{{pr_number}}`\n\n2. Squash-merge via API:\n   `gh api repos/co-l/openfox/pulls/{{pr_number}}/merge -X PUT -f merge_method=squash -f commit_title=\"PR #{{pr_number}}: $(gh pr view {{pr_number}} --json title --jq '.title')\"`\n\n3. Switch back to the original project using the `workspace` tool (target: \"original\")\n\n4. Pull latest develop:\n   `git checkout develop && git pull origin develop --ff-only`\n\n5. Fetch our tag and cherry-pick the fixes onto develop:\n   `git fetch origin refs/tags/review-fixes-{{pr_number}}`\n   `git cherry-pick FETCH_HEAD`\n   `git push origin develop`\n\n6. Verify — both commits visible on origin/develop:\n   `echo \"=== origin/develop after merge ===\"`\n   `git log --oneline origin/develop -3`\n\n7. Clean up:\n   `git push origin --delete refs/tags/review-fixes-{{pr_number}}`\n   `git tag -d review-fixes-{{pr_number}}`\n\n8. Delete the review workspace using the `workspace` tool (delete `review-pr-{{pr_number}}`)\n\nCall step_done() when done.",
      "transitions": [{ "when": { "type": "always" }, "goto": "$done" }]
    }
  ]
}
